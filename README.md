# worker-site

현재 이 저장소는 **정적 사이트 최소 뼈대**만 포함합니다. (`index.html`, `css/style.css`, 빈 `js/`)

이전에 있던 화면·기능은 정리되었으며, **이후 다시 작업 예정**입니다.

GitHub / Vercel 연결은 그대로 두고, 이 레포를 기준으로 점진적으로 확장하면 됩니다.

## 로컬에서 보기

이 폴더를 루트로 정적 서버를 띄우거나 `index.html`을 브라우저로 엽니다.
